.DS_Store

# Local symlinks pointing into $HOME — non-portable
gitconfig_sl
zshrc_sl
zprofile_sl

# Machine-local overrides
*.local
private-commands.sh

# Claude Code state (sessions, history, telemetry, plugin caches)
.claude
