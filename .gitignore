.DS_Store

# Contains [user] name/email — scrub into ~/.gitconfig.local before tracking
gitconfig

# Machine-local overrides
*.local
private-commands.sh

# Claude Code state (sessions, history, telemetry, plugin caches)
.claude
